spring:
  application:
    name: board_bcrypt
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:board_bcrypt}
    username: ${DB_USERNAME:board_bcrypt}
    password: ${DB_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true